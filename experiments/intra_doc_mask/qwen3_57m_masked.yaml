max_seq_len: 4096

model:
  arch: "qwen3"
  n_layers: 8
  n_heads: 8
  n_kv_heads: 4
  d_model: 512
  vocab_size: 50257
  dropout: 0.0
  rope_theta: 500000.0
  qk_norm: true

data:
  dataset: "openwebtext"
  tokenizer_path: "tokenizers/custom_bpe_50k"
  data_dir: "data/"
  val_split: 0.01
  num_workers: 0
  eot_token_id: 0

training:
  batch_size: 4
  gradient_accumulation_steps: 8
  max_steps: 20000
  mixed_precision: "bf16"
  backend: "torch"
  activation_checkpointing: false
  grad_clip: 1.0
  checkpoint_dir: "checkpoints/intra_doc_mask/qwen3_57m_masked"
  checkpoint_every: 5000
  eval_every: 500
  eval_steps: 100
  intra_doc_mask: true

optimizer:
  name: "adamw"
  lr: 6e-4
  weight_decay: 0.1
  betas: [0.9, 0.95]

scheduler:
  name: "cosine"
  warmup_steps: 2000
  min_lr: 6e-5

logging:
  wandb_project: "pretrain-intra-doc-mask"
  wandb_run_name: "qwen3-57m-masked"
  log_every: 10
